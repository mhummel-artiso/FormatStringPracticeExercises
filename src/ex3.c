#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
// Aufgabe 3: Lies den String als String aus.
// Tipp: Was braucht der String-Format-Identifier als Parameter?
// Sei schlau und nutze, was du kriegst.

char str[] = "hack\n";
char *ptr = str;

printf(argv[1]);
return 0;
}